<!--pages/list/list.wxml-->

<view>
  <view class="list">
    <navigator
     wx:for="{{list}}"
     wx:key="{{item.list}}"
     url="{{'../item/item?id='+item.id+'&api=movie'}}">
      <product item="{{item}}"  />
    </navigator>
  </view>
</view>
